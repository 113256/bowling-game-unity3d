using System;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;


    [TestFixture]
    public class ActionMasterTest
    {/*
		private ActionMaster.Action tidy = ActionMaster.Action.Tidy;
		private ActionMaster.Action reset = ActionMaster.Action.Reset;

		Player testplayer1 = new Player ("testplayer1");
		Player testplayer2 = new Player ("testplayer2");
		
		PlayerQueue queue =new PlayerQueue();
		private ActionMaster actionMaster;
		private PinSetter pinsetter;

		//code below runs every single time a test runs
		[SetUp]
		public void Setup(){
		//dont do GameObject.findObjectofType<ActionMaster>(); because this is a normal class, no monodevelop
			actionMaster = new ActionMaster ();
			pinsetter = new PinSetter ();
			actionMaster.queue.Enqueue (testplayer1);
			//pinsetter.getStandingPins ();
		}

		[Test]
		public void tidyTest(){
			
			Assert.AreEqual(tidy, actionMaster.Bowl(8));
			
		}

		[Test]
		public void resetTest(){
			
			//actionMaster.queue.Enqueue (testplayer1);
		Debug.Log ("testtest"+ pinsetter.getStandingPins ());
			actionMaster.Bowl (1);
			ActionMaster.Action action=	actionMaster.Bowl (1);
		Assert.AreEqual(reset, action);
			
		}*/
	}